/**
 * Sunucu (auth.ts authorize()) ve istemci (login formu) arasında paylaşılan
 * sabitler. Bu dosya asla Prisma/NextAuth sunucu kodu import ETMEMELİDİR -
 * client component'lerden güvenle import edilebilmesi bu şarta bağlıdır.
 */
export const TWO_FACTOR_REQUIRED = "TWO_FACTOR_REQUIRED";
